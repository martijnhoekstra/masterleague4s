/*
package masterleague4s
package config

import pureconfig._
import pureconfig.error.ConfigReaderFailures

import scala.concurrent.duration.Duration

case class Credentials(user: String, pass: String)
case class FullConfiguration(credentials: Option[Credentials], delay: Duration)

object Configuration {

  val config: Either[ConfigReaderFailures, FullConfiguration] = loadConfig[FullConfiguration]
}
 */
